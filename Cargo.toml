[package]
name = "mahoraga"
version = "0.1.0"
edition = "2024"

[features]
serde_json = ["dep:serde_json"]

[dependencies]
serde_json = { version = "1", optional = true, features=["std"], default-features = false }

[dev-dependencies]
crossterm = "0.29.0"
ratatui = "0.30.2"
serde_json = "1"
ratatui-textarea = "0.9.2"
