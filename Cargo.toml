[package]
name = "mahoraga"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "repr"

[features]
serde_json = ["dep:serde_json"]

[dependencies]
serde_json = { version = "1", optional = true, features=["std"], default-features = false }
